// postcss.config.mjs
//
// RECONSTRUCTED, framework-necessary: Tailwind CSS v4 is DOCUMENTED
// (DEBUG_LOG.md: "Tailwind v4"). Tailwind v4 requires its PostCSS
// integration via the separate @tailwindcss/postcss package (a
// mechanic of Tailwind v4 itself, not an Atlas-specific decision).
// No other PostCSS plugins are documented anywhere.

const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
