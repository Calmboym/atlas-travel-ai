"""Atlas AI layer — provider-independent (ARCHITECTURE.md §2, §4).

Kept as a top-level package sibling to backend/, not nested inside it,
per DEBUG_LOG.md's documented Architecture Decision ("ai/ package
independent from backend/app/"). ADDED (first real content) —
ATLAS-P1-CHAT-03; prompts/, agents/, schemas/, evaluations/ existed
only as empty scaffold directories (.gitkeep) before this task.
"""
